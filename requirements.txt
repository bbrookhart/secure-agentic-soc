# ---------------------------------------------------------------------------
# Agentic SOC - runtime dependencies
# Pinned to compatible minor ranges: reproducible builds without dependency rot.
# ---------------------------------------------------------------------------

# --- Orchestration -------------------------------------------------------
langgraph>=0.2.60,<0.3
langgraph-checkpoint-sqlite>=2.0.1,<3
langchain-core>=0.3.29,<0.4
langchain-ollama>=0.2.2,<0.3

# --- Typed state / validation --------------------------------------------
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# --- Retrieval -----------------------------------------------------------
chromadb>=0.5.20,<0.6

# --- Analyst UI ----------------------------------------------------------
streamlit>=1.40,<2

# --- Misc ----------------------------------------------------------------
httpx>=0.27,<0.29
